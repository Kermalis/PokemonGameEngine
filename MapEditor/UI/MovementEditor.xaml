<UserControl xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:Kermalis.MapEditor.UI;assembly=MapEditor"
        x:Class="Kermalis.MapEditor.UI.MovementEditor" Padding="2">
  <UserControl.Resources>
    <ui:MovementImage Name="MovementImage" x:Key="MovementImage" />
  </UserControl.Resources>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition />
      <ColumnDefinition Width="5" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>
    <Border Grid.Column="0" Margin="0,0,2,0" Background="{DynamicResource ThemeBorderLowColor}" BorderBrush="{DynamicResource ThemeBorderMidBrush}" BorderThickness="{DynamicResource ThemeBorderThickness}">
      <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Content="{StaticResource MovementImage}" />
    </Border>
    <GridSplitter Grid.Column="1" />
    <Border Grid.Column="2" Margin="2,0,0,0" Padding="2" Background="{DynamicResource ThemeBorderLowColor}" BorderBrush="{DynamicResource ThemeBorderMidBrush}" BorderThickness="{DynamicResource ThemeBorderThickness}">
      <StackPanel Orientation="Vertical" Spacing="5">
        <StackPanel Orientation="Horizontal" Spacing="5">
          <RadioButton Content="Passage" IsChecked="{Binding PassageShown, ElementName=MovementImage}" />
          <RadioButton Content="Elevation" IsChecked="{Binding !PassageShown, ElementName=MovementImage}" />
        </StackPanel>
        <CheckBox Content="Allow Occupancy" IsChecked="{Binding Passage, ElementName=MovementImage, Converter={StaticResource MovementImage}, ConverterParameter=O}" IsVisible="{Binding PassageShown, ElementName=MovementImage}" />
        <CheckBox Content="Southwest Passage" IsChecked="{Binding Passage, ElementName=MovementImage, Converter={StaticResource MovementImage}, ConverterParameter=SW}" IsVisible="{Binding PassageShown, ElementName=MovementImage}" />
        <CheckBox Content="Southeast Passage" IsChecked="{Binding Passage, ElementName=MovementImage, Converter={StaticResource MovementImage}, ConverterParameter=SE}" IsVisible="{Binding PassageShown, ElementName=MovementImage}" />
        <CheckBox Content="Northwest Passage" IsChecked="{Binding Passage, ElementName=MovementImage, Converter={StaticResource MovementImage}, ConverterParameter=NW}" IsVisible="{Binding PassageShown, ElementName=MovementImage}" />
        <CheckBox Content="Northeast Passage" IsChecked="{Binding Passage, ElementName=MovementImage, Converter={StaticResource MovementImage}, ConverterParameter=NE}" IsVisible="{Binding PassageShown, ElementName=MovementImage}" />
        <NumericUpDown Minimum="0" Maximum="255" ClipValueToMinMax="True" Value="{Binding SelectedElevation, ElementName=MovementImage}" IsVisible="{Binding !PassageShown, ElementName=MovementImage}" />
      </StackPanel>
    </Border>
  </Grid>
</UserControl>